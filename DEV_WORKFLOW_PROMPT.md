# 邻光 NeighborGlow 全流程开发提示词（开发 → 测试 → 美化 → 再开发）

> 依据：飞书 PRD v1.1（13 章全文已提取至 `prd_text.md`）
> 目标：对照 PRD MVP P0 范围深度开发，界面精美细致、风格严格统一。

---

## 总原则

1. **设计系统先行**：先建立全局设计令牌，再逐页套用，禁止继续散落硬编码色值/圆角。
2. **PRD 闭环为准**：描述问题 → AI 关键追问 → 生成诊断 → 用户确认或修正 → 分级行动方案 → 选择表达方式 → 模拟训练 → 现实行动 → 结果反馈。
3. **降级必须可用**：无真实 AI 密钥时本地规则引擎 100% 走通全流程。
4. **安全优先**：红色案例不进入模拟训练，直接进入安全响应。
5. **每阶段完成即验证**，发现问题立即进入下一轮修复（再开发循环）。

---

## 设计令牌（PRD 11.4 设计系统规范，必须严格遵循）

| 维度 | 规范 |
|---|---|
| 主色 | 暖琥珀 `#E8A33D`（深色模式 `#F0B85C`） |
| 背景 | 移动端奶油白 `#FFF9F0`；桌面工作台纯白 `#FFFFFF` |
| 风险色 | 绿 `#4CAF50` 低 / 黄 `#FFC107` 中 / 橙 `#FF9800` 高 / 红 `#F44336` 安全 |
| 文字 | 主 `#2D2A26` / 次 `#6B6560` / 辅 `#9E9893` |
| 字体 | 系统无衬线（PingFang SC / Noto Sans SC / system-ui）；标题 600、正文 400 |
| 圆角 | 卡片 16px、按钮 12px、输入框 12px、标签 8px |
| 间距 | 8px 基准网格；页面边距移动 20px / 桌面 32px；卡片内边距 16px；卡片间距 12px |
| 图标 | 线性图标 2px 描边圆角端点；风险等级用填充式图标 |

落地方式：新建 `frontend/src/styles/design-tokens.css`（CSS 变量），`main.js` 全局引入；提供公共工具类（`.ng-card` `.ng-btn-primary` `.ng-btn-secondary` `.ng-badge` `.risk-*` `.ng-empty` `.ng-skeleton` `.ng-degrade-banner`）。

---

## 阶段一：功能开发（对照 PRD P0 逐页补齐）

### 居民端（移动端，480px 居中）
1. **问题描述**（SubmitRequest）：已实现多轮对话收集 + 确认后创建案例，保持；补充场景快捷入口（深夜噪音/停车/装修/宠物等 8 类）、语音转文字入口、安全词汇即时触发风险确认。
2. **AI 诊断**（DiagnosisResult）：
   - 替换原生 `alert()` 为内联 Toast；
   - 输出结构对齐 PRD：问题概况、关系画像、情绪与需要、风险判断、判断依据、核心洞察；
   - 低置信度（<0.6）显示"不确定"标识；
   - 红色风险：确认后跳转安全响应页，而非行动方案。
3. **安全响应页**（新增，红色专用）：清晰说明风险、停止鼓励直接接触、建议离开危险环境、联系可信任人员/机构、紧急提示 110/120、帮助整理事实但不作法律结论。
4. **行动方案**（ActionPlan）：补齐 PRD 字段——当前目标、行动顺序、推荐沟通方式、不建议行为、升级条件、**事实记录建议**、**可立即执行的下一步**。
5. **沟通文案**（MessageGeneration）：6 种格式（微信/当面提纲/门口便条/管理者协助/社区协调/情况说明）× 5 种语气（温和友善/清晰直接/正式克制/简短/长辈友好）；结构：称呼+事实+影响+请求+邀请回应+礼貌结束；标注"AI 生成，仅供参考"。
6. **模拟训练**（SimulationTraining）：邻居/管理者/群居民三类角色 × 开放/防御/回避/强硬四种风格；对方不能一开始就道歉接受；每轮教练反馈（事实是否具体、动机推断、绝对化语言、请求是否清晰、是否给回应空间）；结束后教练总结卡（评分+进步点+建议+「再练一次」「生成最终文案」双按钮）。
7. **行动记录与跟进**（FollowUp）：记录已发送/已沟通/对方同意/暂无回应/改善/加重/已联系管理者；状态时间轴；24h/3天/7天提醒节点。
8. **异常状态**（PRD 明确要求）：
   - 首次使用空状态：温暖引导 + "第一次使用？从描述你遇到的问题开始" + 3 个高频场景快捷入口；
   - AI 加载骨架屏 + 进度文案（"正在分析情况…" "正在组织表达…"）；
   - AI 降级：顶部黄色提示条"AI 服务繁忙，已切换基础模式"；
   - 网络异常：离线提示 + 重试按钮；
   - 筛选无结果："没有符合条件的案例" + 清除筛选。

### 社区管理者工作台（桌面端）
1. **今日概览**：新增案例/待首响/等待跟进/本周改善/橙色风险/红色安全/平均首响时间，指标卡片 + 趋势图 + 风险分布。
2. **案例队列**：按风险等级/类型/状态/区域/等待时间/重复发生筛选，风险色标表格。
3. **AI 回复助手**：首次确认/补充问题/进度说明/协调邀请/结果通知/回访，工作人员确认后才可发送（预览态）。
4. **趋势页**：问题类型趋势、风险分布、高频时段、改善率。

---

## 阶段二：测试

1. 前端编译零错误（`npm run build` 或 dev 无报错）。
2. API 冒烟：注册/登录/创建案例/对话引导（"你好"不创建案例 ✅ 已验证）/诊断/确认修正/方案/文案/模拟/行动记录/反馈。
3. 红色案例：输入威胁类词汇 → 触发风险确认 → 不进入模拟训练 → 显示安全响应。
4. 降级模式：`.env` 设 `llm_enabled=false` 全流程仍可走通。
5. 桌面端与移动端（375–430px）布局均可正常操作。
6. PRD 11.6 验收清单逐项核对（深夜噪音→黄色风险→修正→三步方案→三种文案→防御型邻居不立即道歉→逐轮反馈→行动记录→Dashboard 联动→红色终止模拟→无密钥可走通→双端可用）。

---

## 阶段三：美化（精美细致、风格统一）

1. 全部 20 个视图统一套用 `design-tokens.css`：消除 6/8/10/20px 等杂散圆角，卡片一律 16px、按钮/输入框 12px、标签 8px。
2. 卡片体系：白底 + 16px 圆角 + 柔和阴影（`0 2px 8px rgba(45,42,38,0.05)`）+ 1px 暖边框 `#EDE5D8`。
3. 主视觉卡：琥珀渐变（`#FDE8C8 → #FFF3E0`）用于诊断摘要、目标卡、首页 Hero。
4. 风险徽章：填充式，四级色严格一致；列表左侧风险色条。
5. 微交互：按钮按压反馈、页面过渡、卡片入场渐显、骨架屏微光动画、置信度条平滑过渡。
6. 文字层级：页面标题 20/600、卡片标题 16/600、正文 14/400、辅助 12-13；行高 1.6。
7. 空状态插画感：用柔和渐变 + 大号图标 + 温暖文案，杜绝纯文字空白。
8. 移动端底部导航、桌面端侧边栏视觉统一（琥珀主色高亮态）。

---

## 阶段四：再开发（打磨循环）

1. 浏览器逐页复测完整闭环（描述→诊断→方案→文案→模拟→记录→反馈→工作台联动）。
2. 记录美化阶段发现的残留不一致并修复（循环回阶段一/三）。
3. 每个阶段完成即 `git commit`（GitHub push 需用户 PAT，本地提交不受影响）。
4. 最终产出：风格完全统一、PRD P0 全覆盖、双端可用的演示版本。

---

## 环境信息

- 项目根目录：`neighborglow/`（FastAPI + Vue3 + Vite）
- 后端：`http://localhost:8000`（uvicorn --reload，venv Python）
- 前端：`http://localhost:5173`（npm run dev）
- AI：DeepSeek-V3.2 已配置（`.env`，`llm_enabled=true`），规则引擎兜底
- PRD 全文：工作目录 `prd_text.md`（16907 字符，13 章）
- 测试账号：`test/test123`（resident）
